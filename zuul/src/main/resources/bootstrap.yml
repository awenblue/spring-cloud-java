server:
  port: 38200

spring:
  profiles:
    active: ${active}
  application:
    name: zuul
  cloud:
    config:
      uri: http://admin:admin123@127.0.0.1:38100
      label: ${active}
      profile: ${active}



