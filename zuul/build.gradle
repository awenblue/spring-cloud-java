dependencies {
    testCompile depends.springBootTest

    compile project(':common')

    compile depends.eurekaServer
    compile depends.hystrixDashboard
    compile depends.ribbon
    compile depends.zuul
    compile depends.configStart
}